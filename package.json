 {
    "name": "ghost-blog",
    "dependencies": {
      "ghost-cli": "^1.24.0"
    },
    "scripts": {
      "start": "ghost start --no-setup",
      "build": "ghost install --no-setup --no-stack --no-prompt"
    }
  }
